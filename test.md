# Test Markdown File

This is a test file to verify that:

1. Telescope can preview files without errors
2. Markdown preview works with `<leader>P`
3. Basic syntax highlighting works

## Code Example

```python
def hello():
    print("Hello from Neovim!")
```

The Windows-compatible telescope previewer should display this content correctly.